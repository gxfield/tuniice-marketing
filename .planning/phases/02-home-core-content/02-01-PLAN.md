---
phase: 02-home-core-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Hero.astro
  - src/components/sections/ProductTeasers.astro
  - src/components/sections/SocialLinks.astro
  - src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "Home page displays brand name 'tuniice' in oversized responsive typography"
    - "Home page shows tagline 'music. software. hardware.'"
    - "Three product teaser cards (iOS Apps, Hardware, Web Tools) are visible with different accent colors"
    - "Social links to Bandcamp and SoundCloud are visible and clickable"
  artifacts:
    - path: "src/components/sections/Hero.astro"
      provides: "Hero section with brand name and tagline"
      contains: "tuniice"
    - path: "src/components/sections/ProductTeasers.astro"
      provides: "Product preview cards grid"
      contains: "Card"
    - path: "src/components/sections/SocialLinks.astro"
      provides: "Social link buttons"
      contains: "Button"
    - path: "src/pages/index.astro"
      provides: "Home page composing all sections"
      contains: "Hero"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/sections/Hero.astro"
      via: "import and render"
      pattern: "import Hero"
    - from: "src/components/sections/ProductTeasers.astro"
      to: "src/components/ui/Card.astro"
      via: "import and render with accent variants"
      pattern: "import Card"
    - from: "src/components/sections/SocialLinks.astro"
      to: "src/components/ui/Button.astro"
      via: "import and render with href props"
      pattern: "import Button"
---

<objective>
Build the three home page content sections (Hero, Product Teasers, Social Links) and wire them into the single-page index.

Purpose: Establishes the tuniice brand with compelling hero typography, product previews, and social presence -- the core content visitors see.
Output: Three section components and an updated index.astro that composes them into a full home page.
</objective>

<execution_context>
@/Users/greg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-core-content/02-RESEARCH.md

@src/components/ui/Card.astro
@src/components/ui/Button.astro
@src/styles/tokens.css
@src/pages/index.astro

NOTE: Phase 1.2 (Remove Header) will have already executed before this plan runs. Assume:
- Header component is removed from BaseLayout
- BaseLayout renders <main> directly without header
- No margin-top offset for header in main styles
- The layout is full-page, headerless
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create section components (Hero, ProductTeasers, SocialLinks)</name>
  <files>
    src/components/sections/Hero.astro
    src/components/sections/ProductTeasers.astro
    src/components/sections/SocialLinks.astro
  </files>
  <action>
Create the `src/components/sections/` directory and three components:

**Hero.astro:**
- Full-viewport hero section (min-height: 100vh) centered vertically with flexbox
- Brand name "tuniice" in `--font-heading`, weight 900, lowercase, using `clamp(3rem, 12vw, 8rem)` for responsive sizing
- Tagline "music. software. hardware." in `--font-body`, using `clamp(1rem, 3vw, 1.5rem)`, color `--color-text-muted`, letter-spacing 0.2em
- Tight line-height (1) on brand, negative letter-spacing (-0.02em) for that neo-brutalist heavy feel
- Padding: `--space-xl` on sides, `--space-md` gap between brand and tagline

**ProductTeasers.astro:**
- Import Card from `../ui/Card.astro`
- Section with id="products" for anchor linking
- Section heading "What's Coming" in `--font-heading`, `--font-2xl`, weight 900, centered
- 3 Cards in a grid:
  1. `accent="primary"` -- h3 "iOS Apps", p "Music creation tools in development"
  2. `accent="secondary"` -- h3 "Hardware", p "Physical devices coming soon"
  3. `accent="amber"` -- h3 "Web Tools", p "Online utilities launching soon"
- Grid: single column on mobile, `repeat(3, 1fr)` at 768px+, gap `--space-lg`, max-width 1200px centered
- Section padding: `--space-2xl` vertical, `--space-md` horizontal
- Add `data-animate` attribute to each Card element for scroll animation targeting (Plan 02 adds the animation CSS/JS)

**SocialLinks.astro:**
- Import Button from `../ui/Button.astro`
- Section with id="connect" for anchor linking
- Section heading "Connect" in `--font-heading`, `--font-2xl`, weight 900, centered
- Two Buttons in a flex row (centered, wrapping):
  1. `href="https://bandcamp.com/tuniice"` variant="primary" size="lg" -- text "Bandcamp"
  2. `href="https://soundcloud.com/tuniice"` variant="secondary" size="lg" -- text "SoundCloud"
- Flex container: gap `--space-md`, centered, flex-wrap
- Section padding: `--space-2xl` vertical, `--space-md` horizontal
- Add `data-animate` attribute to the links container div

All components use scoped `<style>` blocks with CSS custom properties from tokens.css. No JavaScript needed in these components.
  </action>
  <verify>
    Run `ls src/components/sections/` to confirm all three files exist.
    Run `npx astro check` or `npm run build` to verify no syntax errors.
  </verify>
  <done>
    Three section components exist in src/components/sections/ with correct imports, markup, and scoped styles. Each uses existing UI components (Card, Button) and design tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire sections into index.astro</name>
  <files>src/pages/index.astro</files>
  <action>
Replace the current index.astro content entirely. The new version:

1. Import BaseLayout and all three section components (Hero, ProductTeasers, SocialLinks)
2. Remove the old Card import and inline hero markup
3. Structure as sectioned single-page layout:

```
<BaseLayout title="Home">
  <Hero />
  <ProductTeasers />
  <SocialLinks />
</BaseLayout>
```

4. Remove ALL existing scoped styles from index.astro (the section components own their own styles now). The page file should have zero or minimal scoped styles -- at most a light wrapper if needed.

Keep it minimal. The page is just a composition of section components. No inline styles, no custom CSS. The sections handle their own layout.
  </action>
  <verify>
    Run `npm run build` to verify the site builds successfully.
    Run `npm run dev` and check http://localhost:4321 loads without errors.
  </verify>
  <done>
    index.astro imports and renders Hero, ProductTeasers, and SocialLinks in order. Site builds and displays all three sections on the home page.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Home page shows: hero with "tuniice" brand + tagline, product teasers grid with 3 cards, social links with Bandcamp/SoundCloud buttons
3. Product cards use different accent colors (green, orange, amber)
4. Social links render as anchor tags with correct hrefs
5. Responsive: cards stack on mobile, 3-column on desktop
</verification>

<success_criteria>
- Hero section fills viewport with oversized "tuniice" text and tagline
- Three product teaser cards visible with "iOS Apps", "Hardware", "Web Tools"
- Bandcamp and SoundCloud buttons visible and link to placeholder URLs
- Site builds and deploys without errors
- All content uses existing design tokens and UI components
</success_criteria>

<output>
After completion, create `.planning/phases/02-home-core-content/02-01-SUMMARY.md`
</output>
