---
phase: 03-products-showcase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/products.ts
  - public/images/products/rhythm-forge.svg
  - public/images/products/grid-controller.svg
  - public/images/products/wave-editor.svg
  - src/components/ui/Card.astro
  - src/components/ui/ProductCard.astro
  - src/components/forms/NotifyForm.astro
autonomous: true

must_haves:
  truths:
    - "Product data defines 3 placeholder products with name, slug, tagline, description, category, and image path"
    - "Card component supports muted prop for coming-soon visual treatment"
    - "ProductCard renders product image, name, tagline, category badge, and links to detail page"
    - "NotifyForm submits email to Formspree and shows inline success message"
  artifacts:
    - path: "src/data/products.ts"
      provides: "Typed product data array"
      exports: ["Product", "products"]
    - path: "src/components/ui/Card.astro"
      provides: "Card with muted variant"
      contains: "card-muted"
    - path: "src/components/ui/ProductCard.astro"
      provides: "Product-specific card component"
      contains: "category"
    - path: "src/components/forms/NotifyForm.astro"
      provides: "Email signup form"
      contains: "formspree"
    - path: "public/images/products/rhythm-forge.svg"
      provides: "Geometric product image"
    - path: "public/images/products/grid-controller.svg"
      provides: "Geometric product image"
    - path: "public/images/products/wave-editor.svg"
      provides: "Geometric product image"
  key_links:
    - from: "src/components/ui/ProductCard.astro"
      to: "src/data/products.ts"
      via: "imports Product type"
      pattern: "import.*Product"
    - from: "src/components/forms/NotifyForm.astro"
      to: "Formspree service"
      via: "form action URL"
      pattern: "formspree"
---

<objective>
Create the product data layer and all reusable components for the products showcase: typed product data, geometric SVG product images, extended Card component with muted state, ProductCard component, and NotifyForm email signup component.

Purpose: Provides the foundation (data + components) that pages in Plan 02 will consume.
Output: Product data file, 3 SVG images, 3 components ready for use.
</objective>

<execution_context>
@/Users/greg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-products-showcase/03-CONTEXT.md
@.planning/phases/03-products-showcase/03-RESEARCH.md
@src/components/ui/Card.astro
@src/components/ui/Button.astro
@src/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create product data file and SVG product images</name>
  <files>
    src/data/products.ts
    public/images/products/rhythm-forge.svg
    public/images/products/grid-controller.svg
    public/images/products/wave-editor.svg
  </files>
  <action>
Create `src/data/products.ts` with a TypeScript interface and exported array:

```typescript
export interface Product {
  id: string;
  slug: string;
  name: string;
  tagline: string;
  description: string;
  category: 'app' | 'hardware';
  image: string;
  status: 'coming-soon';
  accent: 'primary' | 'secondary' | 'amber';
}
```

Define 3 placeholder products:
1. "Rhythm Forge" — app, primary accent — "Beat creation reimagined" / drum sequencing and sound design app
2. "Grid Controller" — hardware, secondary accent — "Tactile music control" / MIDI controller with RGB pads
3. "Wave Editor" — app, amber accent — "Shape your sound" / waveform editor and audio processing tool

Each product's `image` field points to `/images/products/{slug}.svg`.

Create 3 SVG files in `public/images/products/`. Each should be a simple geometric composition (viewBox="0 0 400 300") using the design token CSS custom properties for colors. Each SVG should have a distinct composition:
- rhythm-forge.svg: Grid of small rectangles (beat grid pattern) using --color-accent
- grid-controller.svg: Array of circles (pad layout) using --color-accent-warm
- wave-editor.svg: Zigzag/wave polyline pattern using --color-accent-amber

Keep SVGs simple — 10-20 lines max each. Use `currentColor` for strokes and CSS variables for fills where possible. Add a dark background rect matching --color-bg-elevated (#141414).
  </action>
  <verify>
Run `npx astro check` — no TypeScript errors. Verify SVG files exist with `ls public/images/products/`.
  </verify>
  <done>
Product data exports Product interface and products array with 3 items. Three SVG files exist as geometric product imagery.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Card with muted prop and create ProductCard and NotifyForm components</name>
  <files>
    src/components/ui/Card.astro
    src/components/ui/ProductCard.astro
    src/components/forms/NotifyForm.astro
  </files>
  <action>
**Card.astro** — Add `muted` boolean prop to the existing Card component:
- Add `muted?: boolean` to the Props interface (default false)
- Add `card-muted` class when muted is true: `class={\`card card-${accent} ${muted ? 'card-muted' : ''}\`}`
- Add CSS for muted state:
  ```css
  .card-muted {
    filter: grayscale(40%) opacity(75%);
    transition: filter 0.3s ease, transform 0.15s ease, box-shadow 0.15s ease;
  }
  .card-muted:hover {
    filter: grayscale(20%) opacity(90%);
  }
  ```
- Keep ALL existing Card styles unchanged.

**ProductCard.astro** — New component that composes Card to display a product:
- Import Card from './Card.astro' and the Product type from '../../data/products'
- Accept a `product: Product` prop
- Render inside a Card (accent from product.accent, muted=true since all are coming-soon):
  - Product image (`<img>` tag, src=product.image, alt=product.name, class for sizing)
  - Product name as `<h3>`
  - Tagline as `<p>` with muted text color
  - Category badge: small inline element with uppercase text ("APP" or "HARDWARE"), styled with border and the product's accent color
  - "Coming Soon" status text in muted color
- Style the card internals: image takes full width, content stacked vertically, category badge is a small pill-like element (but square corners per neo-brutalist — no border-radius)
- The entire card should be wrapped in an `<a>` tag linking to `/products/${product.slug}` — but the wrapping `<a>` should be applied by the CONSUMER (products page), not baked into ProductCard. ProductCard just renders the card content.

**NotifyForm.astro** — Email signup form component:
- Use a hardcoded Formspree endpoint placeholder: `https://formspree.io/f/YOUR_FORM_ID` — the real ID will be set up later. Use `import.meta.env.PUBLIC_FORMSPREE_ID || 'YOUR_FORM_ID'` to allow env var override.
- Simple HTML form with method="POST":
  - Email input (type="email", required, placeholder="your@email.com", name="email")
  - Hidden _subject field: "tuniice Launch Notification Signup"
  - Submit button using the project's btn styling (inline styles or matching the Button component pattern: border, font-family, uppercase, shadow)
- Add client-side JavaScript for form submission:
  - Intercept submit with addEventListener
  - Use fetch() to POST as FormData
  - On success: hide form, show inline "You're on the list!" message with accent color
  - On error: show "Something went wrong. Try again." message
- Style form as horizontal layout on desktop (input + button side by side), stacked on mobile
- Use existing design tokens for spacing, fonts, borders, colors
  </action>
  <verify>
Run `npx astro check` — no TypeScript errors. Manually verify Card.astro still has all original styles plus new muted class. Check that ProductCard.astro imports Product type. Check NotifyForm.astro has form element with action URL.
  </verify>
  <done>
Card component supports muted boolean prop with grayscale+opacity filter. ProductCard renders product data in a styled card layout with image, name, tagline, and category badge. NotifyForm captures email with inline success/error handling.
  </done>
</task>

</tasks>

<verification>
1. `npx astro check` passes with no errors
2. `npx astro build` completes successfully (components are importable even if no page uses them yet)
3. Product data file exports both the Product type and products array
4. Card.astro has muted prop and associated CSS
5. SVG files are valid and reference appropriate colors
</verification>

<success_criteria>
- src/data/products.ts exports Product interface and products array with 3 products
- Card component has muted variant with CSS filter effect
- ProductCard displays product image, name, tagline, category badge
- NotifyForm has email input with Formspree integration and JS success handling
- 3 geometric SVG product images exist in public/images/products/
- Project builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-products-showcase/03-01-SUMMARY.md`
</output>
