---
phase: 01.2-remove-header-update-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/nav/Header.astro
autonomous: true

must_haves:
  truths:
    - "Site renders without any header element in the DOM"
    - "Main content has balanced top spacing without fixed-header compensation"
    - "Footer still sticks to bottom of viewport on short content pages"
    - "Site builds and deploys without errors"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Headerless page layout with adjusted spacing"
      contains: "Footer"
      not_contains: "Header"
    - path: "src/components/nav/Header.astro"
      provides: "DELETED - must not exist"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/common/Footer.astro"
      via: "Astro import"
      pattern: "import Footer from"
---

<objective>
Remove the Header component from the site and adjust layout CSS for a clean headerless design.

Purpose: The site is a single-page landing page that does not need navigation. Removing the header simplifies the layout and focuses attention on content.
Output: BaseLayout.astro without Header import/usage, adjusted CSS spacing, deleted Header.astro file.
</objective>

<execution_context>
@/Users/greg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/layouts/BaseLayout.astro
@src/components/nav/Header.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Header component and delete file</name>
  <files>src/layouts/BaseLayout.astro, src/components/nav/Header.astro</files>
  <action>
    In src/layouts/BaseLayout.astro:
    1. Remove the import line: `import Header from '../components/nav/Header.astro';`
    2. Remove the `<Header />` element from the body (line 31)
    3. In the `<style>` block, remove the `margin-top` line from the `main` selector:
       Remove: `margin-top: calc(var(--space-lg) + var(--space-md) + var(--border-width));`
    4. Update the `main` padding from `var(--space-lg) var(--space-md)` to `var(--space-xl) var(--space-md)` to add balanced top spacing for the headerless layout
    5. Update the desktop media query padding from `var(--space-xl) var(--space-lg)` to `var(--space-2xl) var(--space-lg)` for generous desktop top spacing

    Then delete the Header component file:
    6. Delete `src/components/nav/Header.astro`
    7. If the `src/components/nav/` directory is now empty, delete it

    Do NOT modify Footer, tokens.css, global.css, or index.astro -- they have no header references.
  </action>
  <verify>
    Run: `grep -r "Header\|site-header\|hamburger" src/ --include="*.astro" --include="*.ts" --include="*.css"`
    Expected: No results (zero matches)

    Run: `ls src/components/nav/Header.astro 2>&1`
    Expected: "No such file or directory"

    Run: `npm run build`
    Expected: Build succeeds with zero errors
  </verify>
  <done>
    - Header import and usage removed from BaseLayout.astro
    - Header.astro file deleted
    - Main element has no margin-top compensation
    - Main element has increased padding (space-xl mobile, space-2xl desktop) for balanced headerless layout
    - Body flexbox layout preserved (footer still pushed to bottom)
    - Build passes with no errors
  </done>
</task>

</tasks>

<verification>
1. `grep -r "Header\|site-header\|hamburger" src/` returns no results
2. `src/components/nav/Header.astro` does not exist
3. `npm run build` succeeds
4. BaseLayout.astro body contains only `<main>` and `<Footer />` (no `<Header />`)
5. Main element CSS has no margin-top property
6. Main element padding is var(--space-xl) on mobile, var(--space-2xl) on desktop
</verification>

<success_criteria>
- Header component is fully removed from the site (file deleted, all references gone)
- Site styling uses increased padding for balanced headerless layout
- Site builds and deploys without errors
- Footer remains at bottom of viewport
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-remove-header-update-styling/01.2-01-SUMMARY.md`
</output>
