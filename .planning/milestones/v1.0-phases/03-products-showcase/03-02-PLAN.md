---
phase: 03-products-showcase
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/pages/products.astro
  - src/pages/products/[slug].astro
  - src/components/sections/ProductTeasers.astro
autonomous: true

must_haves:
  truths:
    - "Products page displays all products in a grid with category badges distinguishing apps from hardware"
    - "Each product card links to its individual detail page"
    - "Product detail pages show product name, image, description, and email signup form"
    - "Detail pages have a Back to Products link for navigation"
    - "Home page product teasers link to the products page"
    - "Sticky CTA on products page provides always-visible access to email signup"
  artifacts:
    - path: "src/pages/products.astro"
      provides: "Products listing page with grid and sticky CTA"
      contains: "ProductCard"
    - path: "src/pages/products/[slug].astro"
      provides: "Dynamic product detail pages"
      contains: "getStaticPaths"
    - path: "src/components/sections/ProductTeasers.astro"
      provides: "Updated home page teasers linking to /products"
      contains: "/products"
  key_links:
    - from: "src/pages/products.astro"
      to: "src/data/products.ts"
      via: "imports products array"
      pattern: "import.*products"
    - from: "src/pages/products/[slug].astro"
      to: "src/data/products.ts"
      via: "getStaticPaths uses products array"
      pattern: "getStaticPaths"
    - from: "src/pages/products.astro"
      to: "src/components/ui/ProductCard.astro"
      via: "renders ProductCard for each product"
      pattern: "ProductCard"
    - from: "src/pages/products/[slug].astro"
      to: "src/components/forms/NotifyForm.astro"
      via: "renders NotifyForm on detail page"
      pattern: "NotifyForm"
    - from: "src/components/sections/ProductTeasers.astro"
      to: "/products"
      via: "card links updated to products page"
      pattern: "href.*products"
---

<objective>
Build the products listing page, dynamic product detail pages, and sticky email signup CTA. Update home page product teasers to link to the new products page.

Purpose: Creates the user-facing pages that showcase upcoming products and capture launch notification signups.
Output: Products page, individual product detail pages via dynamic routing, updated home page links.
</objective>

<execution_context>
@/Users/greg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-products-showcase/03-CONTEXT.md
@.planning/phases/03-products-showcase/03-RESEARCH.md
@.planning/phases/03-products-showcase/03-01-SUMMARY.md
@src/layouts/BaseLayout.astro
@src/pages/index.astro
@src/components/sections/ProductTeasers.astro
@src/styles/tokens.css
@src/styles/animations.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create products listing page with grid and sticky CTA</name>
  <files>
    src/pages/products.astro
  </files>
  <action>
Create `src/pages/products.astro` — the main products listing page:

**Layout:**
- Use BaseLayout with title="Products"
- Page heading: "What's Coming" (h1, section-heading style matching existing patterns)
- Brief intro text below heading: "Apps and hardware in development. Get notified when we launch."

**Product Grid:**
- Import products array from '../../data/products'
- Import ProductCard from '../components/ui/ProductCard.astro'
- Render all products in a responsive grid (same grid pattern as existing cards-grid in ProductTeasers)
- Each product wrapped in an `<a>` tag linking to `/products/${product.slug}` with class="card-link" and data-animate attribute for scroll animation
- Grid: 1 column on mobile, 2 columns at 768px+, 3 columns at 1024px+

**Sticky CTA:**
- Fixed-position element at bottom-right of viewport (bottom-center on mobile)
- Button text: "Get Notified" with accent styling
- On click, smooth-scroll to a NotifyForm section at the bottom of the page (below the grid)
- The NotifyForm section: heading "Stay in the Loop", subtext "Get notified when we launch new products", then the NotifyForm component
- Import NotifyForm from '../components/forms/NotifyForm.astro'
- Sticky CTA should have z-index: 100, use --color-accent background with --color-bg text
- Add neo-brutalist border and shadow to the sticky button
- Include `scroll-margin-bottom: 80px` on focusable elements to prevent sticky CTA from obscuring focused content

**Navigation:**
- Add a "Back to Home" link at the top of the page (simple text link with arrow: "← Home")
- Style as muted text with hover accent color

**Styles:**
- Use existing design tokens for all spacing, fonts, colors, borders
- Page section padding matching existing patterns (--space-2xl vertical, --space-md horizontal)
- Sticky CTA: position fixed, padding, border, shadow, font-family body, uppercase, font-weight 700

**Accessibility:**
- Sticky CTA should have aria-label="Scroll to signup form"
- Signup section should have id="notify" for the scroll target
  </action>
  <verify>
Run `npx astro build` — page generates without errors. Check that /products/ route exists in build output. Verify the page imports ProductCard and NotifyForm.
  </verify>
  <done>
Products page renders all products in a responsive grid. Sticky CTA scrolls to signup form section at page bottom. Navigation link returns to home page.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create product detail pages and update home page teasers</name>
  <files>
    src/pages/products/[slug].astro
    src/components/sections/ProductTeasers.astro
  </files>
  <action>
**[slug].astro** — Dynamic product detail pages using Astro's getStaticPaths:

```astro
---
import { products } from '../../data/products';
// ... other imports

export function getStaticPaths() {
  return products.map((product) => ({
    params: { slug: product.slug },
    props: { product }
  }));
}

const { product } = Astro.props;
---
```

**Detail page layout (inside BaseLayout):**
- "← Back to Products" link at top (links to /products)
- Product name as h1
- Product image (large, full-width within max container)
- Category badge (same styling as ProductCard badge — "APP" or "HARDWARE")
- "Coming Soon" status indicator
- Product description as paragraph
- Divider (simple border-top line)
- NotifyForm section with heading "Get Notified" and subtext "Be the first to know when {product.name} launches."
- Import and render NotifyForm component

**Styles:**
- Max content width narrower than listing page (e.g., max-width: 700px, centered)
- Product image: full width of container, border matching design system
- Back link: muted text, hover shows accent color
- Use existing design tokens throughout
- Add data-animate to key sections for scroll animation

**ProductTeasers.astro** — Update home page section:
- Change all card `<a>` href values from "#products" to "/products"
- This makes the home page teasers link to the new products listing page
- Keep everything else the same (card accents, content, animations, styles)
  </action>
  <verify>
Run `npx astro build` — check that product detail pages generate (should see products/rhythm-forge, products/grid-controller, products/wave-editor in output). Verify ProductTeasers links point to /products. Run `npx astro check` for type safety.
  </verify>
  <done>
Each product has a detail page at /products/{slug} with name, image, description, and email signup form. Detail pages have Back to Products navigation. Home page product teasers link to /products page.
  </done>
</task>

</tasks>

<verification>
1. `npx astro build` completes successfully
2. Build output includes /products/index.html, /products/rhythm-forge/index.html, /products/grid-controller/index.html, /products/wave-editor/index.html
3. Products page shows all 3 products in grid layout
4. Each product card links to its detail page
5. Detail pages show product info and NotifyForm
6. Home page teasers link to /products
7. Sticky CTA is visible and scrolls to signup section
8. Back navigation works on products and detail pages
</verification>

<success_criteria>
- Products listing page at /products shows all products in responsive grid
- Category badges distinguish apps from hardware
- Sticky CTA provides always-visible signup access
- Each product has detail page at /products/{slug}
- Detail pages include NotifyForm for email capture
- Home page product teasers link to /products
- All pages build without errors
- Navigation between pages works (home -> products -> detail -> back)
</success_criteria>

<output>
After completion, create `.planning/phases/03-products-showcase/03-02-SUMMARY.md`
</output>
