---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/pages/index.astro
  - src/pages/products.astro
  - src/pages/music.astro
  - src/pages/about.astro
  - vercel.json
autonomous: false

must_haves:
  truths:
    - "All four pages (/, /products, /music, /about) render with consistent layout"
    - "Navigation links work across all pages"
    - "Site builds to static HTML and deploys to Vercel"
    - "Every viewport (mobile, tablet, desktop) shows correct responsive behavior"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Home page shell"
      contains: "BaseLayout"
    - path: "src/pages/products.astro"
      provides: "Products page shell"
      contains: "BaseLayout"
    - path: "src/pages/music.astro"
      provides: "Music page shell"
      contains: "BaseLayout"
    - path: "src/pages/about.astro"
      provides: "About page shell"
      contains: "BaseLayout"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "Layout import and wrap"
      pattern: "import.*BaseLayout"
    - from: "src/pages/products.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "Layout import and wrap"
      pattern: "import.*BaseLayout"
---

<objective>
Create page shells for all routes and deploy to Vercel.

Purpose: Complete the foundation by wiring pages to the layout system and deploying. After this plan, the site is live with working navigation across all pages, ready for content in Phase 2.
Output: Four page shells using BaseLayout, deployed to Vercel as a static site.
</objective>

<execution_context>
@/Users/greg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create page shells and deploy to Vercel</name>
  <files>src/pages/index.astro, src/pages/products.astro, src/pages/music.astro, src/pages/about.astro</files>
  <action>
    Create four page files, each importing and wrapping content in BaseLayout:

    **index.astro** (Home):
    - Title: "Home"
    - Content: centered section with "tuniice" as large heading (--font-3xl), a short tagline paragraph ("music. software. hardware." in --color-text-muted), and a Button linking to /products ("Explore" text)
    - Uses Card component to show a brief "Coming Soon" teaser

    **products.astro** (Products):
    - Title: "Products"
    - Content: heading "Products", paragraph "Coming soon." in --color-text-muted
    - Minimal placeholder — content comes in Phase 3

    **music.astro** (Music):
    - Title: "Music"
    - Content: heading "Music", paragraph "Coming soon." in --color-text-muted
    - Minimal placeholder — content comes in Phase 4

    **about.astro** (About):
    - Title: "About"
    - Content: heading "About", paragraph "Coming soon." in --color-text-muted
    - Minimal placeholder — content comes in Phase 4

    All pages should have a container div with responsive padding (--space-md on mobile, --space-xl on desktop) and max-width 1200px centered.

    Then deploy to Vercel:
    - Run `vercel --yes` to deploy (link project if first time)
    - If auth is needed, create a checkpoint for the user to authenticate
    - No vercel.json needed for default static Astro — Vercel auto-detects
    - Verify the deployment URL loads correctly
  </action>
  <verify>
    1. `npm run build` succeeds
    2. All four pages exist in dist/ output
    3. `npm run dev` — navigate to /, /products, /music, /about — all render with header, content, footer
    4. Navigation links work across all pages
    5. Vercel deployment URL loads and serves the site
  </verify>
  <done>All four pages render with BaseLayout (header, content, footer). Navigation works across pages. Site deployed to Vercel and accessible via URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete foundation: Astro site with neo-brutalist design system, reusable components (Button, Card, Header/Nav, Footer), four page shells, deployed to Vercel.</what-built>
  <how-to-verify>
    1. Open the Vercel deployment URL
    2. Check Home page: dark background, neon green "tuniice" logo, Space Grotesk/Mono fonts, "Explore" button with hard shadow
    3. Click hamburger menu (mobile) — nav panel slides in with page links
    4. Press Escape — menu closes
    5. Resize to desktop width — nav shows inline links, hamburger disappears
    6. Navigate to /products, /music, /about — each has consistent header/footer
    7. Hover over buttons and cards — lift effect with shadow change
    8. Check footer at bottom of page
    9. Verify overall neo-brutalist aesthetic: hard shadows, thick borders, bold type, dark mode
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` produces static output with all four pages
2. All navigation links resolve correctly
3. Vercel deployment is live and accessible
4. Responsive behavior correct on mobile and desktop
5. Neo-brutalist styling consistent across all pages
</verification>

<success_criteria>
- Four pages render with BaseLayout
- Navigation works across all pages (mobile hamburger + desktop inline)
- Site deployed to Vercel and accessible via public URL
- Human verification confirms neo-brutalist aesthetic and responsive behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
