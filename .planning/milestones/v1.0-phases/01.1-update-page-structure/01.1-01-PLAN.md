---
phase: 01.1-update-page-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/about.astro
  - src/pages/music.astro
  - src/pages/products.astro
  - src/components/nav/Header.astro
  - src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "Only index.astro exists in src/pages/ — no about, music, or products pages"
    - "No links to /about, /music, or /products exist anywhere in the codebase"
    - "Site builds successfully with no broken links or references"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Single landing page with no broken links"
    - path: "src/components/nav/Header.astro"
      provides: "Header with simplified navigation (no links to removed pages)"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/nav/Header.astro"
      via: "BaseLayout includes Header"
      pattern: "No href to /products, /music, /about"
---

<objective>
Remove all pages except index.astro and clean up every reference to deleted pages across the codebase.

Purpose: Convert multi-page site to single-page site as a structural simplification before building out real content.
Output: A single-page site with no broken links or dead references.
</objective>

<execution_context>
@/Users/greg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/index.astro
@src/components/nav/Header.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete extra pages and clean up references</name>
  <files>
    src/pages/about.astro
    src/pages/music.astro
    src/pages/products.astro
    src/components/nav/Header.astro
    src/pages/index.astro
  </files>
  <action>
    1. Delete the following files:
       - src/pages/about.astro
       - src/pages/music.astro
       - src/pages/products.astro

    2. Update src/components/nav/Header.astro:
       - Remove the three nav links to /products, /music, and /about
       - Keep only the Home link: `<a href="/">Home</a>`
       - All hamburger menu logic, CSS, and JS remain unchanged (the nav still works, just has fewer items)

    3. Update src/pages/index.astro:
       - Remove the `<Button href="/products" ...>Explore</Button>` line (it links to a deleted page)
       - The Button component import can also be removed since it is no longer used
       - Keep the hero section (brand name + tagline) and the Card component as-is
  </action>
  <verify>
    - Run `ls src/pages/` and confirm only `index.astro` exists
    - Run `grep -r "/products\|/music\|/about" src/` and confirm zero matches
    - Run `npm run build` and confirm it succeeds with no errors
  </verify>
  <done>
    Only index.astro exists in src/pages/. No references to /about, /music, or /products remain in any source file. Site builds cleanly.
  </done>
</task>

</tasks>

<verification>
1. `ls src/pages/` shows only `index.astro`
2. `grep -r "/products\|/music\|/about" src/` returns no matches
3. `npm run build` completes successfully
4. No broken imports or unused component references
</verification>

<success_criteria>
- The src/pages/ directory contains only index.astro
- Zero references to /about, /music, or /products in any file under src/
- The site builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-update-page-structure/01.1-01-SUMMARY.md`
</output>
