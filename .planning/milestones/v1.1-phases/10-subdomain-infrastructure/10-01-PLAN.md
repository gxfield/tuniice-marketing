---
phase: 10-subdomain-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bluepriint/package.json
  - bluepriint/astro.config.mjs
  - bluepriint/tsconfig.json
  - bluepriint/public/favicon.svg
  - bluepriint/src/styles/tokens.css
  - bluepriint/src/styles/global.css
  - bluepriint/src/styles/animations.css
  - bluepriint/src/pages/index.astro
autonomous: true
requirements: [SUB-02, SUB-03, SUB-04]
user_setup:
  - service: formspree
    why: "Separate form for bluepriint launch notifications"
    env_vars:
      - name: PUBLIC_FORMSPREE_ID
        source: "Formspree Dashboard -> Create new form titled 'bluepriint launch notifications' -> Copy form ID"

must_haves:
  truths:
    - "Running `npm run build` in bluepriint/ produces dist/index.html with no errors"
    - "The built HTML contains #ffbf00 amber accent, not #00ff41 green"
    - "The page has hero, features, CTA, and footer sections with placeholder content"
    - "The page uses Space Grotesk and Space Mono fonts from bunny.net"
    - "Neo-brutalist styling (hard borders, colored box-shadows, zero radius) is visible"
  artifacts:
    - path: "bluepriint/package.json"
      provides: "Standalone Astro project with own dependencies"
      contains: '"astro"'
    - path: "bluepriint/src/styles/tokens.css"
      provides: "Copied design tokens with amber accent override"
      contains: "--color-accent: #ffbf00"
    - path: "bluepriint/src/pages/index.astro"
      provides: "Single landing page with hero, features, CTA, footer"
      min_lines: 80
  key_links:
    - from: "bluepriint/src/pages/index.astro"
      to: "bluepriint/src/styles/tokens.css"
      via: "CSS import"
      pattern: "import.*styles/tokens"
    - from: "bluepriint/src/pages/index.astro"
      to: "bluepriint/src/styles/global.css"
      via: "CSS import"
      pattern: "import.*styles/global"
    - from: "bluepriint/src/pages/index.astro"
      to: "bluepriint/src/styles/animations.css"
      via: "CSS import"
      pattern: "import.*styles/animations"
---

<objective>
Scaffold the bluepriint/ Astro project as a standalone subdirectory with copied design tokens (amber accent override) and build the complete landing page with hero, features, CTA email signup, and footer sections.

Purpose: Creates the bluepriint product site codebase that will be deployed to bluepriint.tuniice.com. The amber accent and shared neo-brutalist design system establish brand coherence while the distinct color makes it immediately recognizable as a different product.

Output: A buildable Astro project in bluepriint/ with a single landing page containing placeholder content for the bluepriint master clock app.
</objective>

<execution_context>
@/Users/greg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-subdomain-infrastructure/10-CONTEXT.md
@.planning/phases/10-subdomain-infrastructure/10-RESEARCH.md

<interfaces>
<!-- Key files to copy/adapt from main site. Executor should reference these directly. -->

From src/styles/tokens.css (copy, then override accent):
```css
:root {
  --color-accent: #00ff41;        /* CHANGE to #ffbf00 */
  --color-accent-amber: #ffbf00;  /* This IS the target value */
}
@media (prefers-color-scheme: light) {
  :root {
    --color-accent: #0f7a3a;      /* CHANGE to #b98300 */
    --color-accent-amber: #b98300; /* This IS the target value */
  }
}
```

From src/styles/global.css (copy as-is):
```css
/* Modern CSS reset, body background with dot grid, heading/link/image resets, ::selection with accent */
```

From src/styles/animations.css (copy as-is):
```css
/* [data-animate] fade-in on scroll, stagger delays, prefers-reduced-motion override */
```

From src/components/forms/NotifyForm.astro (adapt pattern — key changes):
```astro
<!-- Change _subject to "bluepriint Launch Notification Signup" -->
<!-- Use DOMContentLoaded instead of astro:page-load (no ClientRouter) -->
<!-- Remove the astro:page-load reinit line -->
<!-- Use separate Formspree form ID (env var or placeholder) -->
```

From src/components/common/Footer.astro (adapt pattern — key changes):
```astro
<!-- Replace nav links with single link back to tuniice.com -->
<!-- Change copyright to "tuniice" (parent brand) -->
<!-- Keep neo-brutalist border-top, same layout tokens -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold bluepriint Astro project with amber design tokens</name>
  <files>
    bluepriint/package.json,
    bluepriint/astro.config.mjs,
    bluepriint/tsconfig.json,
    bluepriint/public/favicon.svg,
    bluepriint/src/styles/tokens.css,
    bluepriint/src/styles/global.css,
    bluepriint/src/styles/animations.css
  </files>
  <action>
    Create the `bluepriint/` directory at the repo root as a standalone Astro 5 project.

    1. **bluepriint/package.json** — Create with name "bluepriint", type "module", scripts (dev/build/preview), and astro ^5.16.16 dependency. Match the exact Astro version from the root project's package.json.

    2. **bluepriint/astro.config.mjs** — Minimal config: `import { defineConfig } from 'astro/config'; export default defineConfig({ output: 'static' });`

    3. **bluepriint/tsconfig.json** — `{ "extends": "astro/tsconfigs/strict", "include": [".astro/types.d.ts", "**/*"], "exclude": ["dist"] }`

    4. **bluepriint/public/favicon.svg** — Create a simple geometric SVG favicon using amber (#ffbf00) as the primary color. Keep it minimal (a simple shape like a square or circle with the amber accent).

    5. **bluepriint/src/styles/tokens.css** — Copy the main site's `src/styles/tokens.css` verbatim, then change TWO values:
       - In `:root` block: `--color-accent: #00ff41` becomes `--color-accent: #ffbf00`
       - In `@media (prefers-color-scheme: light) :root` block: `--color-accent: #0f7a3a` becomes `--color-accent: #b98300`
       - Leave ALL other token values unchanged (typography, spacing, borders, shadows, transitions all stay the same)

    6. **bluepriint/src/styles/global.css** — Copy the main site's `src/styles/global.css` as-is with no modifications.

    7. **bluepriint/src/styles/animations.css** — Copy the main site's `src/styles/animations.css` as-is with no modifications.

    8. Run `cd bluepriint && npm install` to generate node_modules and package-lock.json.

    9. Run `cd bluepriint && npm run build` to verify the project builds (it will be an empty site at this point, which is fine — Astro generates a default page).

    IMPORTANT: Do NOT add ClientRouter or any transition:animate directives. This is a single-page static site.
    IMPORTANT: Do NOT add effects.css — no interactive effects for bluepriint v1 per user decision.
    IMPORTANT: Do NOT use `npm create astro@latest` — it conflicts with existing repo. Create files manually.
  </action>
  <verify>
    <automated>cd /Users/greg/ai/tuniice-marketing/bluepriint && npm run build</automated>
  </verify>
  <done>bluepriint/ directory exists with package.json, astro.config.mjs, tsconfig.json, favicon, and three CSS files. tokens.css has #ffbf00 accent in dark mode and #b98300 in light mode. `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Build bluepriint landing page with hero, features, CTA, and footer</name>
  <files>bluepriint/src/pages/index.astro</files>
  <action>
    Create `bluepriint/src/pages/index.astro` as a self-contained single page (no layout component needed for a single-page site).

    **Page structure — all in one file:**

    1. **Frontmatter:** Import the three CSS files (tokens.css, global.css, animations.css). No other imports.

    2. **Head:** Standard meta tags, viewport, bunny.net font link (Space Grotesk 700,900 + Space Mono 400,700), description "bluepriint — master clock and tempo control for live performance", title "bluepriint | tuniice", favicon link.

    3. **Hero section:**
       - Product name "bluepriint" as large heading (font-heading, font-3xl or larger with clamp for responsiveness)
       - Tagline: "Master clock and tempo control for live performance"
       - Brief description (1-2 sentences): Focus on what it does — tempo-locked arrangements, master clock sync, designed for live electronic performance. Pro tool tone, serious and precise.
       - Add `data-animate` to hero elements for scroll-triggered fade-in

    4. **Features section:**
       - 3-4 features presented as a grid (2 columns on desktop, 1 on mobile)
       - Each feature: heading + short description
       - Placeholder features reflecting bluepriint's domain: "Master Clock" (sync all gear to one source), "Arrangement Engine" (program tempo changes and cue points), "Live Control" (real-time adjustments during performance), "Session Export" (capture your set for studio work)
       - Use neo-brutalist card styling: border, accent box-shadow, dark elevated background
       - Add `data-animate` to each feature card for staggered reveal

    5. **CTA / Email signup section:**
       - Heading: "Get notified when bluepriint launches"
       - Adapt the NotifyForm pattern inline (no separate component for single-page site):
         - Form with action pointing to Formspree: `https://formspree.io/f/${import.meta.env.PUBLIC_FORMSPREE_ID || 'YOUR_FORM_ID'}`
         - Hidden _subject: "bluepriint Launch Notification Signup"
         - Email input with placeholder, submit button "Get Notified"
         - Success/error message divs
         - Inline script using DOMContentLoaded (NOT astro:page-load) for async form submission
       - Neo-brutalist button styling with amber accent shadow

    6. **Footer:**
       - Border-top with accent color
       - Link back: "tuniice.com" linking to https://tuniice.com
       - Copyright: current year + "tuniice"
       - Simple flex layout matching main site footer pattern

    7. **Scroll animation script:**
       - IntersectionObserver for `[data-animate]` elements
       - Use `DOMContentLoaded` event (NOT astro:page-load — there is no ClientRouter)
       - threshold: 0.15, rootMargin: '0px 0px -10% 0px'
       - Same pattern as main site BaseLayout

    **Styling (scoped in `<style>` block):**
    - body: flex column, min-height 100vh (push footer to bottom)
    - main: flex 1, responsive padding (space-lg/space-md mobile, space-xl/space-lg desktop), max-width 1400px
    - Hero: generous vertical spacing, large heading
    - Features grid: CSS grid, gap spacing, card styling with border + accent shadow
    - CTA section: centered, form with flex layout (column mobile, row desktop)
    - Footer: border-top, flex layout, muted text
    - All spacing/colors/fonts via CSS custom properties from tokens.css

    IMPORTANT: Pro tool tone throughout — serious, precise, technical. Like Teenage Engineering or Ableton product pages. Not a generic coming-soon template.
    IMPORTANT: Text only — no product screenshots, mockups, or images.
    IMPORTANT: No header/navigation — standalone single page per user decision.
    IMPORTANT: No ClientRouter, no transition:animate, no effects.css imports.
  </action>
  <verify>
    <automated>cd /Users/greg/ai/tuniice-marketing/bluepriint && npm run build && grep -q "ffbf00" dist/index.html && grep -q "bluepriint" dist/index.html && grep -q "Master clock" dist/index.html && echo "PASS: Build succeeds with amber accent and expected content"</automated>
  </verify>
  <done>bluepriint/src/pages/index.astro exists and builds successfully. The page contains: hero with "bluepriint" heading and tagline, features grid with 4 placeholder features, email signup CTA form, and footer with tuniice.com link. All styled with neo-brutalist amber accent (#ffbf00). `npm run build` produces dist/index.html containing amber color values and expected content.</done>
</task>

</tasks>

<verification>
1. `cd /Users/greg/ai/tuniice-marketing/bluepriint && npm run build` — succeeds with no errors
2. `grep "#ffbf00" bluepriint/dist/index.html` — amber accent present in output
3. `grep "#00ff41" bluepriint/src/styles/tokens.css` — should NOT appear (green accent fully replaced)
4. `grep "bluepriint" bluepriint/dist/index.html` — product name in output
5. `grep "DOMContentLoaded" bluepriint/src/pages/index.astro` — correct event listener (not astro:page-load)
6. `grep "ClientRouter" bluepriint/src/pages/index.astro` — should NOT appear
</verification>

<success_criteria>
- bluepriint/ is a standalone Astro project that builds independently
- The page uses amber (#ffbf00) accent everywhere the main site uses green (#00ff41)
- Landing page has hero, features, CTA email signup, and footer sections
- Content has pro tool tone with placeholder bluepriint product info
- No ClientRouter, no interactive effects, no canvas animations
- `npm run build` in bluepriint/ passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-subdomain-infrastructure/10-01-SUMMARY.md`
</output>
