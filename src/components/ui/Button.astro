---
// Button component with neo-brutalist styling
interface Props {
  variant?: 'primary' | 'secondary';
  size?: 'sm' | 'md' | 'lg';
  href?: string;
}

const { variant = 'primary', size = 'md', href } = Astro.props;
const Tag = href ? 'a' : 'button';
---

<Tag
  class={`btn btn-${variant} btn-${size}`}
  href={href}
  {...href ? {} : { type: 'button' }}
>
  <slot />
</Tag>

<style>
  .btn {
    display: inline-block;
    border: var(--border-width) solid var(--color-text);
    background: var(--color-bg);
    color: var(--color-text);
    font-family: var(--font-body);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    cursor: pointer;
    text-decoration: none;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }

  /* Variant styles */
  .btn-primary {
    box-shadow: var(--shadow-offset-md) var(--shadow-offset-md) 0 var(--color-accent);
  }

  .btn-secondary {
    box-shadow: var(--shadow-offset-md) var(--shadow-offset-md) 0 var(--color-accent-warm);
  }

  /* Size variants */
  .btn-sm {
    padding: var(--space-xs) var(--space-md);
    font-size: var(--font-sm);
  }

  .btn-md {
    padding: var(--space-sm) var(--space-lg);
    font-size: var(--font-md);
  }

  .btn-lg {
    padding: var(--space-md) var(--space-xl);
    font-size: var(--font-lg);
  }

  /* Hover and focus states */
  .btn:hover,
  .btn:focus-visible {
    transform: translate(-2px, -2px);
  }

  .btn-primary:hover,
  .btn-primary:focus-visible {
    box-shadow: var(--shadow-offset-lg) var(--shadow-offset-lg) 0 var(--color-accent);
  }

  .btn-secondary:hover,
  .btn-secondary:focus-visible {
    box-shadow: var(--shadow-offset-lg) var(--shadow-offset-lg) 0 var(--color-accent-warm);
  }

  /* Active state */
  .btn:active {
    transform: translate(0, 0);
  }

  .btn-primary:active {
    box-shadow: var(--shadow-offset-sm) var(--shadow-offset-sm) 0 var(--color-accent);
  }

  .btn-secondary:active {
    box-shadow: var(--shadow-offset-sm) var(--shadow-offset-sm) 0 var(--color-accent-warm);
  }
</style>
